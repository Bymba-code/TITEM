const { returnCatchedError } = require("../../../services/controllerServices");

const GET_ALL_LESSONS_ME = async (req, res) => {
  try 
  {
    
  } 
  catch(err) 
  {
    returnCatchedError(err, res);
  }
};

module.exports = GET_ALL_LESSONS_ME;
